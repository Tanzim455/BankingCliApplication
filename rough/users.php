<?php
$users = array();
//First check if array size is greater than 0 or not 
if (count($users) > 0) {

    echo "Need to check whether email exists already and filter email for validation";
}

if (empty($users)) {
    echo "No need for check just filter end enter email and password";
}
